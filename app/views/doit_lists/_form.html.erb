<%= form_for(doit_list) do |f| %>
  <% if doit_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doit_list.errors.count, "error") %> prohibited this doit_list from being saved:</h2>

      <ul>
      <% doit_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
